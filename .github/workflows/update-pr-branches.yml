name: Update PR branches

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  update:
    uses: arthexis/ci-base/.github/workflows/update-pr-branches.yml@v1
